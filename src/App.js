import React from "react";
import CountryTable from "./CountryTable"; // Update the file path to the actual location of your CountryTable component

const App = () => {
  return (
    <div>
      {/* Render other components or content as needed */}
      <CountryTable />
    </div>
  );
};

export default App;
